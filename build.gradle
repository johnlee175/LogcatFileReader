apply plugin: 'java'

jar {
    manifest {
        attributes 'Main-Class': 'com.johnsoft.ui.Reader'
    }
    version '1.0.0'
}

repositories {
    jcenter()
}

dependencies {
    compile files('libs/*.jar')
}
